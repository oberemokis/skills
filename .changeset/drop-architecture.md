---
"oberemokis-skills": major
---

Скилл `architecture` удалён. Он описывал раскладку одной конкретной монорепы, а та перестроилась, и правила из скилла начали спорить с фактическим состоянием проекта. Вопрос «куда положить этот код» больше ни на какой скилл не маршрутизируется: в `core` убран пункт маршрутизации, а условие «задача пересекает слои или пакеты» осталось в таблице допроса со своей причиной. Ссылки из `stack/vue` и `stack/vite` заменены на правило по месту.
